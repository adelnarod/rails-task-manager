<h2>My Tasks</h2>
<ol>
  <% @tasks.each do  |task| %>
    <li>
      <%= link_to task.title, task_path(task)%>
      <%= link_to 'Delete Task', task_path(task), method: :delete, data: {confirm: "Are your sure ?"} %>
    </li>
  <% end %>
</ol>

<%= link_to "Add a new Task",  tasks_new_path %>
